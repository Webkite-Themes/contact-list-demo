<nav class="webkite-results-paginator">
  <a href="#" data-page="1" data-per-page="{{perPage}}" {{#if (equals page 1)}}disabled class="disabled"{{/if}}>&lt;&lt;</a>
  <a href="#" data-page="{{prevPage}}" data-per-page="{{perPage}}" {{#if (equals page 1)}}disabled class="disabled"{{/if}}>&lt;</a>
  {{#each pagesToShow}}
  <a href="#" data-page="{{this}}" data-per-page="{{../perPage}}" class="{{#if (equals this ../page)}}webkite-current-page{{/if}}">
    {{this}}
  </a>
  {{/each}}
  <a href="#" data-page="{{nextPage}}" data-per-page="{{perPage}}" {{#if (equals page pages)}}disabled class="disabled"{{/if}}>&gt;</a>
  <a href="#" data-page="{{pages}}" data-per-page="{{perPage}}" {{#if (equals page pages)}}disabled class="disabled"{{/if}}>&gt;&gt;</a>
</nav>
